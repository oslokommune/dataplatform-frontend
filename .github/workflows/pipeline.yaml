name: Pipeline

on:
  push:
    branches: master

jobs:
  bump:
    name: Bump version
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2 # Fetch code
        with:
          ref: ${{ github.ref }}
      - name: "Automated Version Bump"
        uses: "phips28/gh-action-bump-version@master"
        with:
          tag-prefix: ""
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish:
    name: Publish to github packages and github container registry
    needs: bump
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: echo "::set-env name=RELEASE_VERSION::$(jq -r .version package.json)"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to Github Packages
        uses: docker/login-action@v1 
        with:
          registry: docker.pkg.github.com
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1 
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.CR_PAT }}

      - name: Build and push image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            docker.pkg.github.com/oslokommune/dataplatform-frontend:latest
            docker.pkg.github.com/oslokommune/dataplatform-frontend:${{ env.RELEASE_VERSION }}
            ghcr.io/oslokommune/ok-origo-dataplatform/dataplatform-frontend:latest
            ghcr.io/oslokommune/ok-origo-dataplatform/dataplatform-frontend:${{ env.RELEASE_VERSION }}